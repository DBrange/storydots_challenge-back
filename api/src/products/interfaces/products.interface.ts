export interface IProductEntity{
  name: string;
  description: string;
  image: string;
  price: number;
}

// id
// name
// description
// image_url (pueden traer imágenes que no están hosteadas en la app)
// price
